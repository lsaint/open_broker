
# 依赖
    uwsgi
    Django 1.3
    Pycrypto
    MySQL-python
    python-memcached

    supervisor
    haproxy

#
执行主目录下的configure.py

conf文件夹内的配置文件 带deploy前缀的为生产环境配置文件
不带前缀的为当前使用的配置文件，默认为测试配置
执行完configure.py后，这些文件将被deploy前缀的文件替换
从而切换为生产环境配置

启动测试环境则不需要执行configure.py

#
停止broker：
    sudo sh kill.sh

#
启动broker:
    sudo sh start.sh


